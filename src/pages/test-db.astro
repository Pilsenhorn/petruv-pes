---
import BaseLayout from "../layouts/BaseLayout.astro";
import { supabase } from "../lib/supabase";

const { data, error } = await supabase.auth.getSession();
---

<BaseLayout title="DB test">
  <pre>{JSON.stringify({ data, error }, null, 2)}</pre>
</BaseLayout>